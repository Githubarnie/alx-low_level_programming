#include <stdio.h>
/**
 *
 * main - print the alphabet starting at a
 *
 * Description: using the main function
 * this program prints "Programming is positive, zero, negative
 * Return: 0
 */
int main(void)
{
char alpha;
for (alpha = 'a' ; alpha <= 'z' ; ch++)
{
putchar(alpha);
}
putchar('\n');
return (0);
}
